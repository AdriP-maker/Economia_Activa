<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ══════════════════════════════════════════
         Base Theme — Material 3 + Economía Activa
         ══════════════════════════════════════════ -->
    <style name="Base.Theme.Economia_Activa" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary Brand Color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorPrimaryContainer">@color/colorPrimaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/colorOnPrimaryContainer</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryContainer">@color/colorSecondaryContainer</item>

        <!-- Tertiary -->
        <item name="colorTertiary">@color/colorTertiary</item>
        <item name="colorTertiaryContainer">@color/colorTertiaryContainer</item>

        <!-- Surface & Background -->
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorSurfaceVariant">@color/colorSurfaceVariant</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorOnSurfaceVariant">@color/colorOnSurfaceVariant</item>

        <!-- Error -->
        <item name="colorError">@color/colorDanger</item>

        <!-- Window -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Shape — global roundness 12dp -->
        <item name="shapeAppearanceSmallComponent">@style/ShapeSmall</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeMedium</item>
        <item name="shapeAppearanceLargeComponent">@style/ShapeLarge</item>

        <!-- Default Button style -->
        <item name="materialButtonStyle">@style/Widget.EconomiaActiva.Button</item>

        <!-- CardView style -->
        <item name="materialCardViewStyle">@style/Widget.EconomiaActiva.CardView</item>
    </style>

    <style name="Theme.Economia_Activa" parent="Base.Theme.Economia_Activa" />

    <!-- ── Splash / Biometric theme (dark header) ── -->
    <style name="Theme.Economia_Activa.Biometric" parent="Base.Theme.Economia_Activa">
        <item name="android:windowBackground">@color/colorPrimaryDarker</item>
        <item name="android:statusBarColor">@color/colorPrimaryDarker</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- ══ SHAPES ══ -->
    <style name="ShapeSmall">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <style name="ShapeMedium">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <style name="ShapeLarge">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>

    <!-- ══ BUTTONS ══ -->
    <style name="Widget.EconomiaActiva.Button" parent="Widget.Material3.Button">
        <item name="cornerRadius">16dp</item>
        <item name="android:minHeight">52dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@color/white</item>
        <item name="elevation">0dp</item>
    </style>

    <style name="Widget.EconomiaActiva.Button.Pill" parent="Widget.EconomiaActiva.Button">
        <item name="cornerRadius">26dp</item>
    </style>

    <!-- ══ CARDS ══ -->
    <style name="Widget.EconomiaActiva.CardView" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardBackgroundColor">@color/colorCardBackground</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/colorOutlineVariant</item>
    </style>

    <!-- ══ TEXT APPEARANCES ══ -->
    <style name="TextStyle.Display" parent="TextAppearance.Material3.DisplayMedium">
        <item name="android:textSize">36sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>

    <style name="TextStyle.Headline" parent="TextAppearance.Material3.HeadlineMedium">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>

    <style name="TextStyle.Title" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>

</resources>